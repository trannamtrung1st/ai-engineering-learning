# Output goal

Produce the requested project output while preserving the declared scope and
satisfying the approved plan.

Use this file with `run.output_goal_file` when the goal is longer than is
comfortable inline in YAML. Paths resolve against `run.workspace` (or process
cwd). Mutually exclusive with `run.output_goal`.
