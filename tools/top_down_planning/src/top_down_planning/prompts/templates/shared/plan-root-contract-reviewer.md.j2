- Root contract: when {{ plan_root_item_id }} has active child items, it must not keep the seeded title {{ plan_root_default_title | tojson }} and must have a non-empty outcome that its children genuinely decompose.
