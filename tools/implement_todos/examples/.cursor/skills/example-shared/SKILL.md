# Shared example skill

Follow repository conventions and keep todos metadata accurate:

- Item `status` is orchestrator-owned.
- Checklist entries use `{id, text, done}` and ids are unique within an item.
- Update checklist progress in YAML as work completes.
