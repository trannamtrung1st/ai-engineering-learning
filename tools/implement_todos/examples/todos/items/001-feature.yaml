version: 1

id: TASK-001
title: Add greeting helper
type: feature
status: pending
priority: 100

depends_on:
  - SETUP-001

description: |
  Add a small greeting helper module that returns a personalized greeting string.
  Follow the current repository layout and conventions.

acceptance_criteria:
  - A greeting helper function exists and returns a non-empty string.
  - Basic unit tests cover the happy path.

checklist:
  - id: ck-module
    text: Add greeting helper module with personalized greeting function
    done: false
  - id: ck-tests
    text: Add unit tests for the happy path
    done: false

validation:
  commands: []

context:
  files: []

agent_context:
  review:
    rules:
      - .cursor/rules/example-item-review.mdc

result:
  completed_at: null
  commit_sha: null
  summary: null
