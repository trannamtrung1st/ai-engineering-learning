[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "core-tools"
version = "0.1.0"
description = "Shared infrastructure for agent orchestration tools"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
  "rich>=13.0",
]

[project.optional-dependencies]
dev = [
  "build",
  "pytest>=8.0",
  "pytest-xdist>=3.8",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]
addopts = ["-n", "auto", "--dist", "worksteal"]
