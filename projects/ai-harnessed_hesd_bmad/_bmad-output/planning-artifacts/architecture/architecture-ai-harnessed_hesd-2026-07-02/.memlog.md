---
scope: HESD Workshop Digital Attendance System — initiative spine
purpose: build-substrate for solo agent implementation
altitude: initiative
updated: 2026-07-02T13:18
---

- (decision) decision: Paradigm = modular monolith on Next.js App Router; vertical slices by role (admin/instructor/student), shared domain core in lib/domain. Prevents split frontend/backend repos diverging on check-in rules.
- (decision) decision: Starter = npx create-next-app -e with-supabase (Next.js 16.2.9, TypeScript, Tailwind, @supabase/ssr cookie auth). Prevents hand-rolled auth/session wiring.
- (decision) decision: ORM = Drizzle 0.45.2 on Supabase Postgres; migrations via drizzle-kit. Prevents schema drift between agents.
- (decision) decision: AD-3 All attendance/check-in/audit mutations go through lib/domain use-cases invoked from Server Actions or Route Handlers only; no client direct table writes. Prevents bypass of validation sequence.
- (decision) decision: AD-4 QR tokens server-minted, 30s TTL, bound to active session_id; stored in session_tokens table; instructor display polls GET /api/sessions/{id}/qr-token. Prevents client-forged tokens.
- (decision) decision: AD-5 Check-in orchestrator executeCheckIn() runs validation order 1-5 from check-in-validation.md atomically in one transaction. Prevents partial writes on failed checks.
- (decision) decision: AD-6 Unique partial index on attendance_records(session_id, student_id) where status=checked_in. Prevents duplicate successful check-ins.
- (decision) decision: AD-7 Role in profiles.role enum (admin|instructor|student); enforced middleware + requireRole() server guard. Prevents role surface bleed.
- (decision) decision: AD-8 Realtime via Supabase postgres_changes on attendance_records WHERE session_id=; instructor dashboard only. Prevents polling-only dashboard.
- (decision) decision: AD-9 No public signup; admin creates users via admin API; Supabase Auth admin API or invite flow. Prevents self-registration.
- (decision) decision: AD-10 GPS collected client-side Geolocation API; coords sent with check-in attempt; server computes haversine vs session geofence. Prevents trusting client-only geofence.
- (decision) decision: AD-11 Append-only check_in_attempts table; every attempt logged via same domain function regardless of outcome. Prevents missing audit on failures.
- (decision) decision: AD-12 Deploy Vercel (app) + Supabase hosted (DB/auth/realtime); bootstrap admin via supabase/seed.sql. Prevents undeployable pilot.
- (decision) assumption: Solo builder; no multi-team ownership boundaries needed beyond module folders.
- (decision) assumption: Basic logging (console + Vercel logs); no APM/tracing in MVP.
- (decision) version: Next.js 16.2.9 verified npm 2026-07-02
- (decision) version: drizzle-orm 0.45.2 verified npm 2026-07-02
- (decision) version: Supabase with-supabase starter verified supabase.com/docs 2026-07-02
- (decision) direction: Additional IMPLEMENTATION.md companion for agent loading order and module map.
- (decision) event: fast path selected; solo builder; agent-friendly complete docs
- (decision) AD-13: Domain writes via Drizzle+DATABASE_URL with actor auth in use-case; reads may use Drizzle or Supabase client; RLS defense-in-depth. Prevents mixed write paths.
- (event) spine finalized
- (event) docs sharded: index.md + spine/ (11 sections) + implementation/ (8 sections); root ARCHITECTURE-SPINE.md and IMPLEMENTATION.md are entry stubs
- (decision) AD-1 amended: Monorepo split — Next.js web (UI + Supabase SSR auth + Realtime subscribe) + NestJS API (domain + REST). Domain logic only in api/src/domain/. Prevents two mutation paths (Server Actions vs API).
- (decision) AD-3 amended: All attendance/check-in/audit/roster/account mutations via NestJS REST controllers → domain use-cases. Web calls API with Supabase session JWT; no Drizzle/Supabase writes on domain tables from web. Prevents client bypass.
- (decision) AD-14 [ADOPTED]: Docker Compose at repo root. Profile local: postgres + api (NestJS hot-reload). Profile integration: isolated postgres + api + migrate/seed for automated integration tests. Web runs on host (npm run dev) or optional web service. Supabase Auth/Realtime via supabase start CLI or remote dev project. Prevents divergent local vs CI infra.
- (decision) AD-15 [ADOPTED]: Backend = NestJS 11.1.27 on Node.js 20+. Bootstrap api/ with nest CLI; modules by bounded context (check-in, sessions, accounts, rosters, attendance). Prevents ad-hoc Express handlers.
- (decision) AD-12 amended: Local envelope = docker compose (AD-14) + supabase CLI for auth/realtime. Production: Vercel (web) + hosted API target deferred; Supabase hosted DB/auth/realtime unchanged. Prevents undeployable pilot while API host TBD.
- (version) NestJS @nestjs/core 11.1.27 verified npm 2026-07-02; requires Node.js 20+
- (event) update: backend stack NestJS + docker compose local/integration; user directive
- (event) spine finalized (update: NestJS API + docker compose)
