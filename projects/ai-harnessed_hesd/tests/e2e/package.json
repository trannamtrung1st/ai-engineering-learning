{
  "name": "@attendly/e2e",
  "version": "0.1.0",
  "private": true,
  "description": "Attendly API end-to-end regression — role scope, export, and audit flows",
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test:unit": "vitest run --config vitest.config.ts src/fixtures",
    "test:e2e": "npm run build -w @attendly/domain && vitest run --config vitest.config.ts src/scenarios",
    "test": "npm run test:e2e"
  },
  "devDependencies": {
    "@attendly/config": "*",
    "@attendly/domain": "*",
    "@types/node": "^22.10.2",
    "fastify": "^5.2.0",
    "pg": "^8.13.1",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
